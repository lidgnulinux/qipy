# Build recipe for .
#

# Exit immediately on any error
set -e

program=
version=
release=0
description=""
homepage=""
license=""

tarname=
fetch=""

build() 
{
	unpack "${tardir}/$tarname"
	
	cd "$srcdir"
	
	# Set sane permissions
	chmod -R u+w,go-w,a+rX-s .

	# Do build stuffs below
}
